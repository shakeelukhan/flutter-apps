import 'package:flutter/widgets.dart';
import 'package:flutter/material.dart';

class StatelessWidgets {
  Widget text(String data) => Text(data);
  Widget center(Widget widget) => Center(child: widget);
}