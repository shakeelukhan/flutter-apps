import 'package:flutter/widgets.dart';
import 'package:flutter/material.dart';

class StatelessWidgets {
  static Widget text(String data) => Text(data);
  static Widget center(Widget widget) => Center(child: widget);
}
